build:
  docker:
    web: Dockerfile
    web_client: Dockerfile.vue
release:
  image: web
  command:
    - python manage.py migrate
run:
  web: gunicorn printestimate.wsgi
  web_client: nginx -g "daemon off;"