build:
  docker:
    web: Dockerfile.vue
    worker: Dockerfile
release:
  image: worker
  command:
    - python manage.py migrate
run:
  worker: gunicorn printestimate.wsgi
  web: nginx -g "daemon off;"